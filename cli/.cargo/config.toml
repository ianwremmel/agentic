# Cross-compilation linker config. The Apple targets are linked via
# cargo-zigbuild (which supplies its own linker), so only the aarch64 Linux
# cross-linker needs declaring here.
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
