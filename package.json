{
  "name": "@ianwremmel/dispatch",
  "version": "0.0.0",
  "private": true,
  "description": "dispatch CLI — orchestrates engineering work across PRs and trackers",
  "license": "MIT",
  "author": "Ian Remmel",
  "type": "module",
  "bin": {
    "dispatch": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && echo 'bundle step pending (#17)'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^17.6.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
