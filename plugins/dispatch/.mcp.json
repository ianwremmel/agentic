{
  "mcpServers": {
    "dispatch": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/dispatch",
      "args": ["mcp"],
      "env": {
        "DISPATCH_ENTRY": "${CLAUDE_PLUGIN_ROOT}/src/main.mts"
      }
    }
  }
}
